{base_boa_md}

## Agent Edit Strategy

- Aggressiveness: `{aggressiveness}`
- Guidance: {aggressiveness_summary}

### Mode Rules

{aggressiveness_rules}

## BOA Setup Summary

- Train command: `{train_command}`
- Eval command: `{eval_command}`
- Primary metric: `{primary_metric_name}` ({metric_direction})
- Editable paths: {editable_paths}
- Protected paths: {protected_paths}
- Likely optimization surfaces: {optimization_surfaces}

## Caveats

{caveats}
