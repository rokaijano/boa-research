You are configuring BOA Research for an existing repository.

Analyze the repository and return exactly one JSON object that matches the provided schema.

Do not wrap the JSON in markdown fences.

Repository root: {repo_root}

Selected agent preset: {agent_preset}

Selected runner mode: {runner_mode}

Selected coding aggressiveness: {agent_aggressiveness}

Aggressiveness guidance: {aggressiveness_summary}

Repository tree:
{repository_tree}

Context file excerpts:
{context_file_excerpts}

Return a JSON object using this schema:
{repo_analysis_schema}
