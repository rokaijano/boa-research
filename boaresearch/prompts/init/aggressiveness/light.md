Prefer tiny, surgical edits. Avoid broad refactors and preserve surrounding code unless a minimal fix requires more.

- Make the smallest viable diff that can move the primary metric.
- Avoid touching adjacent code unless it is necessary for correctness.
- Prefer local fixes over refactors, renames, or file moves.
